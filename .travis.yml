language: python
python:
  - "3.9.5"  # 3.9.5 main branch
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script:
  - export PYTHONPATH="${PYTHONPATH}:./:../"
  - pytest
